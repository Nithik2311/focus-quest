# Database Configuration
# Use 'db' if running via docker-compose, 'localhost' if running locally
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=focus_quest
DB_PORT=5432

# Server Configuration
PORT=5001

# JWT Secret
JWT_SECRET=your_jwt_secret_here

# Node Environment
NODE_ENV=development
